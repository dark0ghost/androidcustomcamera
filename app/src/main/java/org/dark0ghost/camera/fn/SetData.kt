package org.dark0ghost.camera.fn

import android.content.SharedPreferences

fun setGlobalSettingsFromContext(prefs: SharedPreferences){

}

fun setNewPerf(prefs: SharedPreferences){
    val editor = prefs.edit()
    //editor.putInt(APP_PREFERENCES_COUNTER, counter).apply()
}